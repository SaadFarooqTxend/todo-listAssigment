
import Usercontext from "./UserContext";
import { Children, useState } from "react";
const UserContextProvider=({})=>{
    return(
        <UserContextProvider value={}></UserContextProvider>
        {Children}
        </UserContext.Provider
    
    )
})

